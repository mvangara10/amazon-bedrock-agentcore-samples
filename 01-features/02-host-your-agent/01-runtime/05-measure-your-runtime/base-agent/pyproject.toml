[project]
name = "base-agent"
version = "0.1.0"
description = "Echo-reply agent for AgentCore Runtime container-size benchmarking"
requires-python = ">=3.13,<3.14"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "pydantic>=2.7",  # FastAPI depends on it
]
